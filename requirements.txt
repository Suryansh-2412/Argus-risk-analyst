# Core ML / NLP
torch>=2.2.0
transformers>=4.40.0
sentence-transformers>=3.0.0
xgboost>=2.0.0
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.2.0

# Finance data
yfinance>=0.2.40
finnhub-python>=2.4.19    # free tier — register at finnhub.io
feedparser>=6.0.11        # Google News RSS — no key needed

# Free LLM API (optional upgrade over local flan-t5)
groq>=0.9.0               # free tier — register at console.groq.com

# Paid LLM APIs (optional — only install if you have keys)
# anthropic>=0.29.0
# openai>=1.30.0

# HTTP + retries
httpx>=0.27.0
tenacity>=8.3.0

# Storage
sqlalchemy>=2.0.0

# Config
pyyaml>=6.0.1
python-dotenv>=1.0.0

# Dashboard
streamlit>=1.35.0
plotly>=5.22.0

# Utilities
tqdm>=4.66.0
joblib>=1.4.0
loguru>=0.7.2

# Dev / testing
pytest>=8.2.0
pytest-mock>=3.14.0
